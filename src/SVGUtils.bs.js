// Generated by BUCKLESCRIPT VERSION 2.2.2, PLEASE EDIT WITH CARE
'use strict';

var List = require("bs-platform/lib/js/list.js");
var Pervasives = require("bs-platform/lib/js/pervasives.js");

function translateTransform(x, y) {
  return "translate(" + (Pervasives.string_of_int(x) + (", " + (Pervasives.string_of_int(y) + ")")));
}

function lineToSequence(param) {
  return List.fold_left((function (memo, param) {
                return memo + ("L" + (Pervasives.string_of_int(param[0]) + (", " + Pervasives.string_of_int(param[1]))));
              }), "", param);
}

function linePath(data) {
  if (data) {
    var match = data[0];
    return /* Some */["M" + (Pervasives.string_of_int(match[0]) + ("," + (Pervasives.string_of_int(match[1]) + lineToSequence(data[1]))))];
  } else {
    return /* None */0;
  }
}

exports.translateTransform = translateTransform;
exports.lineToSequence = lineToSequence;
exports.linePath = linePath;
/* No side effect */
